# Oil & Gas Streamlit App - Python Dependencies
# ============================================
# Core packages
streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0,<7.0.0  # 5.x uses scatter_mapbox, 6.x uses scatter_map — both supported via fallback

# Postgres driver (psycopg3 preferred, psycopg2 as fallback)
psycopg[binary,pool]>=3.1.0
# psycopg2-binary>=2.9.9  # Uncomment if psycopg3 not available

# Databricks SDK (for token generation in local dev)
databricks-sdk>=0.118.0

# Utilities
python-dotenv>=1.0.0